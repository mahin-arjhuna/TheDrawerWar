using Unity.Entities;
using UnityEngine;

public struct DAIStaple : IComponentData
{
    // Specific Variables (only staples have these)
    public float speed;
    // Internal Variables (for calculations and logic)
}
